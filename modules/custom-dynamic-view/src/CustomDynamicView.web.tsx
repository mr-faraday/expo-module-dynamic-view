import * as React from 'react';
import { CustomDynamicViewProps } from './CustomDynamicView.types';

export default function CustomDynamicView(props: CustomDynamicViewProps) {
  return <div>Not implemented</div>
}
